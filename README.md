# Furniture

Monorepo with separate frontend and backend.

- **frontend/** – React/Vite UI (run from this folder: `npm install` then `npm run dev`)
- **backend/** – API server (placeholder; add your backend here)

## Quick start

```bash
# Frontend
cd frontend && npm install && npm run dev

# Backend – add your stack and run from backend/
cd backend
```
