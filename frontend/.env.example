# Backend API URL — required for products, categories, stores (no static catalog in the app).
# Development:
VITE_API_URL=http://localhost:4000
# Production: set to your deployed API origin, e.g. https://your-api.example.com

# Admin password (for /admin login). Use a strong secret in production.
VITE_ADMIN_PASSWORD=admin123
